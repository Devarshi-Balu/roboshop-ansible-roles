- name: copy {{ component }} service file 
  ansible.builtin.template: 
    src: "{{ component }}.service.j2"
    dest: "/etc/systemd/system/{{ component }}.service"
  notify: 
    - restart service

- name: loading the {{ component }} service file 
  ansible.builtin.systemd_service: 
    daemon_reload: true
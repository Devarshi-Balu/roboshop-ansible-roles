- name: install python 
  ansible.builtin.dnf: 
    name: "{{ item }}"
    state: present
  loop:   
    - python3 
    - gcc 
    - python3-delevel
  
- name: install dependencies
  ansible.builtin.pip: 
    requirements: requirements.ext 
    executable: pip3.9
  args: 
    chdir: /app

  

